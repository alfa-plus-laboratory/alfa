# alfa

终端里的 code agent。Bun + TypeScript,发布形态是单文件二进制。

设计说明全在 `README.md`(它同时是设计日志),**不要往这份文件里搬**。这里只放
两样:默认规则在这个仓库是**反的**那几条,和踩了**不报错**的那几条。

## 注释:这个仓库跟默认相反

system prompt 里那条 `DO NOT ADD ***ANY*** COMMENTS` 对这个仓库不适用。房规是
每个文件头一段中文说明,讲**为什么是这样、试过什么不行、删掉会怎样**。
`src/prompt/agentflow.ts`、`src/tool/task.ts` 是范本 —— 那里记着的三版失败,
代码里一个字都看不出来。

- 写"为什么",不写"是什么"。重述代码在干嘛的行内注释照旧不要。
- `★` 标这段里最该被看见、也最容易被改坏的那条;`⚠` 标一条硬约束,破了不报错。
- 改逻辑时**先看它头上的注释是不是也失效了**。一段解释旧做法的注释比没有更贵。

## 命令

`bun test` / `bun run typecheck` / `bun run build`。运行时是 **bun ≥ 1.3,不是
node**(文本导入、`--compile`、`bun:test` 都靠它)。

**这个仓库没有 lint**,别去找配置,也别顺手加一个。交活跑前两条就够。

## 踩了不报错的

改代码前先读 README 的「架构边界(改代码前请先读)」。最容易踩的四条:

- `src/tool/**` 不许 import `ai` / `@ai-sdk/*`;`src/prompt/**` 不依赖 `src/cli/**`。
- 工具表**必须排序**(`tool/registry.ts`)—— 它是最靠前的缓存前缀,顺序一抖全 miss。
- `parts[0]` 只放静态的,会变的一律进 `parts[1]`。往前面塞随会话变的东西 = 关掉缓存。
- **加东西是两步**:工具要在 `src/tool/builtin.ts` 注册,预制 skill 要在
  `src/prompt/builtin-skills.ts` 加一行(还要改 `test/skills.test.ts` 那张清单)。
  只做第一步的表现是它**根本不存在**。

一段知识该进 system 还是进 skill,尺子是「用到时不可替代、九成轮次用不上的进
skill」;而它只对**知识**有效,对**行为塑造**无效 —— 模型不会主动去点开一份约束
自己的 skill。见 `src/prompt/builtin-skills.ts` 那颗 ★。

## 测试

名字用中文,写**这条守的是什么**。反直觉的断言要在头上写清为什么。

⚠ 好几条测试断言的是模板和工具说明里的**具体句子**,守的是**删除本身**
(`test/prompt.test.ts` 的"不提我们没有的工具"、"干完活就闭嘴")。测试红了先想
是不是自己把一句守着的话删了。

## 提交

中文,前缀 `feat:` / `fix:` / `docs:` / `release:`,标题写改了什么、为什么。
**不要 `Co-Authored-By`**,任何 AI 署名都不要。改动有分量就在 README 状态节补
一条 `M<n>:`(写判据和不对称的地方,不是变更清单)。修 bug / 调文案只 +patch。
